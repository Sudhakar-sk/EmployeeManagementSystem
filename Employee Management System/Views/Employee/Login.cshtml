@model EmployeeManagement.Core.Models.AdminLogin

@{
    ViewBag.title = "Login";
}
<link href="~/css/Login.css" rel="stylesheet" />
<script src="~/lib/jquery/dist/jquery.js"></script>
@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}

<div class="container">
    <div class="contain">
        <div class="main">
            <p class="sign" align="center">Log in</p>
            @using (Html.BeginForm("Login", "Employee", FormMethod.Post, new { @id = "formDetails", @autocomplete = "off" }))
            {
                <div class="form1">
                    <div class="form-group">
                        @Html.EditorFor(Modal => Modal.UserName, new
                        {
                            HtmlAttributes = new
                            {
                                @name = "UserName",
                                @id = "user",
                                @type = "text",
                                @class = "un",
                                @placeholder = "Username",
                                @maxlength = "50",
                                @align = "center",
                                @*required = "true"*@
}
})
                      
                    </div>

                 
                    <div class="form-group">
                        @Html.EditorFor(Modal => Modal.PassWord, new
                        {
                            HtmlAttributes = new
                            {
                                @name = "Password",
                                @type = "password",
                                @class = "pass",
                                @placeholder = "Password",
                                @maxlength = "50",
                                @align = "center",
                              
}
})
                      
                    </div>

                   
                    <button class="submit" type="submit" align="center" onclick="SubmitDetails()">Log in</button>
                   
                </div>
            }
        </div>
    </div>
</div>
<script>
    $(function () {
        debugger;
        $("#formDetails").validate({
            rules:
            {
                UserName: {
                    required: true,

                },

                PassWord: {
                    required: true,
                    minlength: 4,
                    maxlength: 10
                }


            },
            messages:
            {
                UserName: {
                    required: "Name is required",

                },
                PassWord:
                {
                    required: "Password is required",
                    minlength: "Password should have miminum of 4 characters",
                    maxlength: "Password should have maximum of 10 charaacters"
                }
            }
        });
    });

    function SubmitDetails() {
        if ($("#formDetails").valid()) {
            $("#formDetails").submit();
        }
    }


</script>

@*@using (Html.BeginForm("Login", "Employee", FormMethod.Post, new { @class = "formDetails" }))
{


    <div class="form-group">

        <div>@Html.EditorFor(m => m.UserName, new { @class = "form-control", @placeholder = "Username" })</div>
    </div>

    <div class="form-group">
        <div>@Html.EditorFor(m => m.PassWord, new { @class = "form-control", @type = "password", @placeholder = "password" })</div>
    </div>

    <div class="form-group">
        <button type="submit" class="btn btn-primary form-control">Sign in</button>
    </div>
}*@