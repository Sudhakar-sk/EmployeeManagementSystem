<!--model EmployeeManagement.Core.Models.FileDetails
{
    ViewData["Title"] = "Index";
}
<script src="~/lib/jquery/dist/jquery.js"></script>

{
    <partial name="_ValidationScriptsPartial" />
}-->
<!--using (Html.BeginForm("FileUploads", "Employee", FormMethod.Post, new { id = "Formdetails", enctype = "multipart/form-data" }))
    {
    <div class="form-group">

        <label asp-for="Photo" class="col-form-label"></label>
        <div class="col-md-10">
            <div class="custom-file">
                <div>
                    Html.EditorFor(model => model.Photo, new { class = "form-control custom-file-input" })
                    <label class="custom-file-label">Choose File</label>
                </div>
            </div>
        </div>
    </div>
        <button type="Submit" onclick="SubmitDetails()" class="btn btn-success">Upload</button>
        }-->
<!--<script>
        $(function () {
            $("#Formdetails").validate({
                rules:
                {
                    Photo:
                    {
                        required: true,
                    }
                },
                message:
                {
                    Photo:
                    {
                        required: "Upload File Required"
                    }
                }
            });
        });
        function SubmitDetails() {
            if ($("#Formdetails").valid()) {
                $("#Formdetails").submit();
            }
        }
    </script>-->
<!--<div class="row">
    <div class="col-md-5">
        <form method="post" enctype="multipart/form-data" asp-controller="Employee" asp-action="FileUploads" id="Formdetails">

            <div class="form-group">
                <label asp-for="Photo" class="col-form-label"></label>
                <div class="col-md-10">
                    <div class="custom-file">
                        <input asp-for="Photo" class="form-control custom-file-input" />
                        <label class="custom-file-label">Choose File</label>
                    </div>
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-10">
                    <input class="btn btn-success" type="submit" value="Upload" />
                </div>
            </div>
            section Script{
                <script>
                    $(function () {
                        debugger

                        $('.custom-file-input').on("change", function () {
                            var fileName = $(this).val().split("\\").pop();
                           
                            $(this).next('custom-file-label').html(fileName);
                        });
                    });
                </script>
            }
            </form>
    </div>
</div>-->


<!--<script>
    $(function () {
        debugger
        $('.custom-file-input').on("change", function () {
            var fileName = $(this).val().split("\\").pop();
            $(this).next('custom-file-label').html(fileName);
        });
    });
</script>-->
